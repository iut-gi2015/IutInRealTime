{# src/Iirt/UserBundle/Resources/views/User/ajouter.html.twig #}

{% extends "IirtUserBundle::layout.html.twig" %}

{% block contenu %}
<h3>modifier ajouter un role</h3>
<div class="well">
        nom :{{ users.firstname }}
    <form action="{{ path('iirt_user_role', {'id': users.matricule}) }}" method="post" id="form_connect"  >
         nom :   
        <div class="form-group">
            <select name="role">
                
    {% for role in roles %}
        <option value='{{ role.id }}' >{{ role.name }}</option>
    {% endfor %}
            </select>
        </div>
        <input class="btn btn-primary pullright" type="submit" value="connexion" />

    </form>
</div>

{% endblock %}